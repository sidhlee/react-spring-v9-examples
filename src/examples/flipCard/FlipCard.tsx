import React from 'react';

import { FlipCardContainer } from './flip-card-styles';

type FlipCardProps = {};

const FlipCard = (props: FlipCardProps) => {
  return <FlipCardContainer>FlipCard</FlipCardContainer>;
};

export default FlipCard;
